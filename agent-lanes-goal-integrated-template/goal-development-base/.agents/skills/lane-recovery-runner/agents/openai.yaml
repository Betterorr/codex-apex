interface:
  display_name: "泳道恢复执行器"
  short_description: "替换崩溃或过长的泳道线程并维护注册表和审计接管记录"
  default_prompt: "Use $lane-recovery-runner to replace a broken Agent Lane thread, update registry, archive the old thread, and preserve recovery audit records."
